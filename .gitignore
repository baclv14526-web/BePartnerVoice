# Android build outputs
/app/build/
/build/

# Gradle
.gradle/
local.properties

# Keystore – KHÔNG bao giờ commit file này
*.jks
*.keystore

# IDE
.idea/
*.iml
.DS_Store

# Signing properties (nếu dùng local)
# RELEASE_* sẽ được CI inject, không cần commit
